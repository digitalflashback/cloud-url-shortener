name: Cloud URL Shortener CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Check repository
        run: |
          ls
          echo "Repository checked out successfully"